<resources>
    <!-- Estilo base para los botones de números -->
    <style name="NumberButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">28sp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:background">#ADD8E6</item> <!-- Azul claro -->
        <item name="android:elevation">2dp</item>
        <item name="android:padding">16dp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo para los botones de operadores -->
    <style name="OperatorButton" parent="NumberButton">
        <item name="android:background">#ADD8E6</item> <!-- Azul claro -->
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <!-- Estilo para botones de operaciones (AC, %, +/-) -->
    <style name="OperationButton" parent="NumberButton">
        <item name="android:background">#ADD8E6</item> <!-- Azul claro -->
        <item name="android:textColor">#000000</item>
    </style>
</resources>
